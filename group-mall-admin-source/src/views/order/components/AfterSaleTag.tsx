import Vue, { CreateElement } from "vue";
import { Component } from "vue-property-decorator";

@Component
export default class AfterSaleTag extends Vue {
  public render(h: CreateElement) {
    return <div></div>;
  }
}
